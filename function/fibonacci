#include <iostream>
using namespace std;
int fib(int n){
    if(n<=1){
        return 1;
    }
    return fib(n-2)+fib(n-1);
};
int main() {
    int n;
    cout<<"enter the value"<<endl;
    cin>>n;
    cout<<"the fibonacci of "<<fib(n-2)+fib(n-1)<<endl;
    
    return 0;
}
